<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:key="preferences">

    <PreferenceCategory
        android:title="General">

        <SwitchPreference
            android:key="wifi-only-sync"
            android:title="Only sync over WiFi"
            android:defaultValue="false" />

        <ListPreference
            android:key="week-load-size"
            android:title="The amount of weeks to load"
            android:entries="@array/pref_list_weeks"
            android:entryValues="@array/pref_list_weeks"
            android:defaultValue="3" />
    </PreferenceCategory>
</PreferenceScreen>
